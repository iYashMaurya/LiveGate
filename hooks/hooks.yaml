hooks:
  on_session_start:
    - script: scripts/bootstrap.js
      description: "Initialize memory/runtime/ directory and load baseline"
  pre_tool_use:
    - script: scripts/audit-log.js
      description: "Log every tool invocation for audit trail"
